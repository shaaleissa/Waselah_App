import 'package:flutter/material.dart';

class EtrqaRequest extends StatelessWidget {
  const EtrqaRequest({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}