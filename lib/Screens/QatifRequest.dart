import 'package:flutter/material.dart';

class QatifRequest extends StatelessWidget {
  const QatifRequest({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}