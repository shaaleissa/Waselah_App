import 'package:flutter/material.dart';

class JoodRequest extends StatelessWidget {
  const JoodRequest({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}